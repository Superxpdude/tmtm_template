// Task Enhancements
// Enhancements to the task system introduced with the End Game scenarios. Includes task sharing and new markers

enable = 1; 			// Enable new task features
3d = 1; 				// Use 3d task markers
3dDrawDist = 2000; 		// Range at which to draw markers. Default is 2000
share = 1; 				// Count players assigned to a task and share over the network
propagate = 0; 			// Propagate tasks to subordinates when a group leader changes their active task 
